import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "in.planeduhrm.app",
  appName: "planedu-hrm",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
